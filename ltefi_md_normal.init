#!/bin/bash

. ltefi_md_git.in

main() {
	local PRO_PATH="histb_project_$(date | awk -F ' ' '{print $2$3}')"

	mkdir -p ../${PRO_PATH}
	pushd ../${PRO_PATH}
	echo "#### PROJECT PATH: $(pwd) ####"

	git_clone_md
	git_clone_md_release
	git_clone_md_custom
	git_clone_ap
		
	popd
	exit 0
}

main
